[package]
name = "app-settings"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
directories = "5"

[target.'cfg(windows)'.dependencies]
winreg = "0.52"
